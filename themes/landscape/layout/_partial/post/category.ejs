<% if (post.categories && post.categories.length){ %>
  <div class="article-category">
    <%- list_categories(post.categories, {
      show_count: false,
      class: 'article-category.ejs',
      style: 'none',
      separator: '►'
    }) %>
  </div>
<% } %>